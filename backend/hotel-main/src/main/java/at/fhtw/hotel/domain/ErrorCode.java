package at.fhtw.hotel.domain;

public enum ErrorCode {
    INVALID_INPUT,
    ROOM_NOT_FOUND,
    BOOKING_NOT_FOUND,
    DATES_UNAVAILABLE,
    INVALID_DATE_RANGE,
    EMAIL_MISMATCH,
    INTERNAL_ERROR
}
