spring:
  datasource:
    url: jdbc:mysql://localhost:3306/boutique_hotel?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: hotel
    password: hotel
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
